import{getOpenPRs}from"@/lib/github";import{PRCard as _P}from"./PRCard";export async function PRList(){let _,e=null;try{_=await getOpenPRs()}catch(x){e=x instanceof Error?x.message:"Failed to fetch PRs"}return e?<div className="w-full max-w-3xl text-center py-8"><p className="text-zinc-500">{e}</p><p className="mt-2 text-sm text-zinc-600">Try refreshing the page in a minute.</p></div>:!_||!_.length?<div className="w-full max-w-3xl text-center py-8"><p className="text-zinc-400">No open PRs yet.</p><p className="mt-2 text-sm text-zinc-500">Be the first to submit one!</p></div>:<div className="w-full max-w-3xl space-y-4">{_.map((p,i)=><_P key={p.number}pr={p}rank={i+1}/>)}</div>}
